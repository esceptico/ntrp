From this single memory CLAIM, extract the proper-noun ENTITIES it is about — the
people, organizations, projects, products, or places named in it. These are the
durable nodes the claim should hang off of in the knowledge graph.

- Extract only named entities, written as they would head their own profile
  ("Regina Lin", "Postgres", "the MATS program"). Use the fullest form the claim gives.
- Do NOT extract attributes, dates, generic nouns, or the user themselves.
- One entity per line, plain text, no bullets, no numbering, no preamble.
- If the claim names no durable entity, respond with exactly: NONE

Claim:
{claim}
