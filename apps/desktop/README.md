# ntrp desktop

Small Electron client for the local ntrp server.

```bash
bun install
bun run dev
```

The app connects to `http://localhost:6877` by default and stores the API key in browser local storage for now.
