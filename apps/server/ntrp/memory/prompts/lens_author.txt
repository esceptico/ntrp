You design "lenses" for a personal memory system. A lens declares a named
directory that groups related entities, and tells the extractor how to classify
members and shape each profile.

The user wants a directory for this request:
"{query}"

Below is a sample of the user's actual memory items, so you ground the lens in
what really exists rather than inventing categories. Each line is `[kind] content`.

{grounding}

Design ONE lens for the request. Return strict JSON:
{{
  "slug": "kebab-case-id (lowercase letters, digits, hyphens; start with a letter)",
  "directory": "short human directory name, e.g. 'Northwind engineers'",
  "entity_type": "the kind of thing grouped, e.g. 'person', 'project', 'library', 'decision'",
  "belongs": "1-3 sentences defining who/what belongs in this directory and what to exclude",
  "profile_shape": ["field each member's profile should capture", "..."]
}}

Pick an entity_type that matches the grounding data. Keep belongs criteria
concrete. profile_shape should have 2-4 short fields.
